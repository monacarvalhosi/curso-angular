import { GrifarTextoDirective } from './grifar-texto.directive';

describe('GrifarTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new GrifarTextoDirective();
    expect(directive).toBeTruthy();
  });
});
